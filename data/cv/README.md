# CV folder

**Put your CV PDF here.**

- File name must be: **`cv.pdf`**
- Path: `data/cv/cv.pdf`

Then run:

```bash
npm run ingest-cv
```

This will create `data/cv-chunks.json` (and `data/cv-text.json`) for the RAG pipeline.
