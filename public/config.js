// API base URL for the digital-twin backend (Render or other).
// Leave empty when frontend and API are on the same origin (e.g. local dev).
// For Vercel/Netlify frontend, set to your Render URL: "https://your-app.onrender.com"
window.API_BASE_URL = '';
